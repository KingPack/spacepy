tags:
- "Config"
summary: "Insere forçadamente um artigo."
description: "Essa rota força um insert no banco de dados."
produces:
- "application/json"
responses:
  "200":
    description: "Insere um aritigo."
parameters:
  - name: "id"
    in: "path"
    required: true
    minimum: 1
    maximum: 20000
    type: "integer"

